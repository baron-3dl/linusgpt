docs/voice-blind-test-v2.md written. Honest verdict: FAIL — worse than v1 on aggregate and now unambiguous about why.

WHAT CHANGED FROM v1: All seven drift-fixes (DF-1..DF-7) were applied and the famous corpus was swapped for an obscure sourced corpus. The prompt fixes worked on their targets — across all 24 round-2 AI tells, NOT ONE judge cites markdown, backticks, em-dashes, a verdict-stamp closer, a stereotype-bait opener, or self-censored profanity. DF-1/DF-2/DF-7 are fully solved; DF-5 mostly. The mechanical/rendering layer is fixed.

OBSCURE SOURCED CORPUS (real-recognition is now genuine discrimination, not memorization): R0 get_unaligned/put_unaligned; R1 strncpy-vs-snprintf (road to strscpy); R2 bitfield endianness; R3 IA-64 function-descriptor/caller-loads-GP; R4 inline_hint; R5 push putname into free_bprm for execve tracing; R6 un-pull + "grep'd function used EXACTLY NOWHERE"; R7 free_majors[] -> find_next_zero_bit bitmap. Panel held all 8 as real on mechanics (organic typos, two-dot ".." ellipses, underscore emphasis on grammatical words, grounded procedural anecdotes) at LOWER confidence (57-74) than v1's memorized 86-98 — less sure, still right.

AGGREGATE vs v1: accuracy 88% -> 100% (16/16 by majority; 47/48 = 97.9% by vote); AI-detection 100% -> 100% (24/24, all 8 twins caught 3/3); real-recognition 75% (memorized) -> 100% (discriminated). All targets are ~50% / ~50-60% — failed on every axis. No misclassifications either direction; R5 is the only real item drawing a single AI vote.

RESIDUAL TELLS (the two deep, prompt-resistant clusters now carrying 100% detection): RD-1 cadence/compression floor cited on EVERY AI item (A0-A7) — "too tidy, too symmetric, pedagogically complete, texture-free," clean N-beat skeleton with snark sprinkled on top, explains the failure chain like a tutorial; real Linus reacts/rambles/dwells/weaves contempt through the content. This is EXACTLY the drift DF-3 was written to kill, and DF-3 ran and did not move it. RD-2 plausible-but-wrong mechanism / confabulation (A1 i_rwsem vs dentry-lock, A2 incoherent "clamp causes unbounded drift," A4 schedule-while-atomic mislabeled as deadlock, A6 u32-vs-unsigned-long type contradiction revealing invented code) — DF-6 made the model reach for precision but it cannot land it, because it generates against an imagined patch. RD-3 (minor) absence of organic typo/two-dot-ellipsis texture.

PASS/BORDERLINE/FAIL: FAIL, unambiguous — and more informative than v1 because both confounds (surface tells, memorization) are removed, leaving cadence as the single unanimous tell.

AI-JUDGE-PROXY CAVEAT (kept explicit, load-bearing): judges are Opus-class AI, a pre-screen not the certifier. They over-index on cadence-tidiness in a way a human skimming a roast card on a clearly-labeled parody product may not; a human panel could be more forgiving. The human blind panel (the human voice panel; see TASKS.md) remains the final gate. Sequence: fine-tune -> re-screen AI panel -> human panel certifies.

FINE-TUNING NOW WARRANTED: v1 said fine-tune only if cadence drift persists after DF-1..DF-7. v2 observed exactly that — systematic (100% of AI items), prompt-resistant (DF-3 targeted it and failed), with the confounds that could explain it away now gone. Recommend proceeding to fine-tune on obscure real LKML reviews (teach the cadence floor, not the tropes). One optional cheap pre-condition: a maximally aggressive few-shot prompt (2-3 real exemplars + explicit sentence-length-variance / "react, don't explain") as a final prompt-fixability sanity check before spending fine-tune budget — but the evidence threshold is met. The next lever is weights, not words.

File: /home/baron/projects/linusgpt/docs/voice-blind-test-v2.md
